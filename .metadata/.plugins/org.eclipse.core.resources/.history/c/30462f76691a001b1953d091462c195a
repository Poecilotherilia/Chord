package com.Chord.data;

import com.Chord.tool.SHA1;

public class Node {
	private Address addr;
	private long hashCode;
	public Node(String ip,int port) {
		addr = new Address(ip,port);
		this.hashCode = SHA1.GetHash(ip+":"+port.toString());
	}
}
